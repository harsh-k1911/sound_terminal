# Terminal Music Player

A terminal-based MP3 music player built with Node.js.

⚠️ **Work in progress** — Features being added incrementally.

## Setup

Install dependencies:
```bash
npm install
```

Run the player:
```bash
node index.js
```

Or use the command-line alias:
```bash
npm start
```
